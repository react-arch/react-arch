---
"create-react-arch-app": patch
---

Now that the `react-arch` CLI is published, scaffolded projects get a `check` script (`react-arch check src/House.tsx`) and the `react-arch` dev dependency, so the validate loop is one `npm run check` away. Pin the `@react-arch/*` libraries to `^0.2.0`.
