declare module "virtual:react-arch-root" {
  import type { ComponentType } from "react";
  /** The project's registration root (renders <Composition> entries). */
  const Root: ComponentType;
  export default Root;
}
